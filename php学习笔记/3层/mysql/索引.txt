联合索引 test_col1_col2_col3 实际建立了(col1)、(col1,col2)、(col,col2,col3)三个索引。

联合唯一索引和空值： 只要某一列为空，其他字段完全相同，也不会引起唯一索引冲突。但是此列在使用时不会使用索引。

主键索引，是一种特殊的唯一索引，一个表只能有一个主键，不允许有空值。
唯一索引，索引列的值必须唯一，但允许有空值。如果是组合索引，则列值的组合必须唯一

